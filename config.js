/* Optional deployment defaults.
 *
 * Set dbUrl to your Firebase Realtime Database URL to turn on cross-device
 * sync for everyone who opens the site, without them pasting anything.
 * Leave it empty and the app runs locally until someone fills it in under
 * Settings, or opens a share link that carries it.
 *
 * Example:
 *   window.PSC_CONFIG = { dbUrl: "https://poker-santa-cruz-default-rtdb.europe-west1.firebasedatabase.app" };
 */
window.PSC_CONFIG = {
  dbUrl: ""
};
